{
  "name": "subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for the Chainlink hackathon project",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy",
    "create-local": "graph create --node http://localhost:8020/",
    "remove-local": "graph remove --node http://localhost:8020/",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001",
    "test": "graph test",
    "clean": "rm -rf build generated"
  },
  "keywords": [
    "subgraph",
    "thegraph",
    "chainlink"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.80.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}